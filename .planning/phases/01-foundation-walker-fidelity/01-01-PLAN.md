---
phase: 01-foundation-walker-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/hooks/useHorizontalScroll.ts
  - src/app/page.tsx
autonomous: true
requirements:
  - FND-01
  - FND-07
user_setup: []
must_haves:
  truths:
    - "User can traverse the entire homepage horizontal scroll journey without unreachable whitespace or dead scroll states"
    - "User experiences smooth scroll choreography without broken pinning, dead scroll states, or hydration failures"
  artifacts:
    - path: "src/lib/hooks/useHorizontalScroll.ts"
      provides: "Horizontal scroll animation hook with proper GSAP lifecycle, debouncing, and resize stability"
      min_lines: 60
    - path: "src/app/page.tsx"
      provides: "Homepage with robust horizontal scroll orchestration, dynamic measurement, and image-load resilience"
      min_lines: 200
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/hooks/useHorizontalScroll.ts"
      via: "useHorizontalScroll hook invocation"
      pattern: "useHorizontalScroll\\(\\{\\s*containerRef,\\s*trackRef\\s*\\}\\)"
    - from: "src/lib/hooks/useHorizontalScroll.ts"
      to: "gsap.context"
      via: "creates GSAP context with ScrollTrigger"
      pattern: "gsap\\.context\\(\\)"
---

<objective>
Harden the core horizontal scroll-jacking mechanism that drives the Walker-style homepage journey. Fix measurement stability, resize handling, image-load resilience, and type correctness to eliminate dead scroll zones and unreachable content.

Purpose: The horizontal scroll is the foundational interaction pattern for the entire homepage. If it breaks, every downstream section is inaccessible. Phase 1 success criterion FND-01 (homepage scroll journey) and FND-07 (smooth choreography) depend on this being rock-solid.

Output: Refactored useHorizontalScroll hook with deterministic travelDistance calculation; hardened page.tsx scroll container with image-load-aware refresh; eliminated NodeJS.Timeout type drift; debounce aligned to Walker spec (200ms).
</objective>

<execution_context>
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/workflows/execute-plan.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/templates/summary.md
</execution_context>

<context>
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/PROJECT.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/ROADMAP.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/REQUIREMENTS.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/STATE.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/phases/01-foundation-walker-fidelity/01-CONTEXT.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/phases/01-foundation-walker-fidelity/01-PATTERNS.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/codebase/STRUCTURE.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/codebase/CONCERNS.md

# Existing files this plan modifies
@D:/Users/Avinash/Documents/StElizabethWebsite/src/lib/hooks/useHorizontalScroll.ts
@D:/Users/Avinash/Documents/StElizabethWebsite/src/app/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor useHorizontalScroll hook with Walker-aligned stability</name>
  <files>src/lib/hooks/useHorizontalScroll.ts</files>
  <behavior>
    - Behavior 1: Hook calculates travelDistance = track.scrollWidth - window.innerWidth on mount and after every resize
    - Behavior 2: Debounce delay is 200ms (Walker spec for buttery-smooth, prevents jitter)
    - Behavior 3: On resize, animationContext.revert() is called, then createScrollAnimation() re-runs, then ScrollTrigger.refresh() is called
    - Behavior 4: No NodeJS.Timeout type in browser code — uses number for timer IDs
    - Behavior 5: Cleanup removes resize listener and reverts GSAP context
  </behavior>
  <action>
    Update src/lib/hooks/useHorizontalScroll.ts to implement:
    1. Change NodeJS.Timeout to number (browser timer return type)
    2. Adjust debounce wait from 150ms to 200ms per Walker SOP-001 (heavy premium feel)
    3. Ensure createScrollAnimation closure recalculates travelDistance fresh each call
    4. Add missing invalidateOnRefresh: true to ScrollTrigger config (ensures DOM re-read after refresh)
    5. Keep anticipatePin: 1 (SMPTE-compliant pinning)
    6. Document the hook pattern with JSDoc comments
    7. Do NOT change the overall hook architecture — keep acceptor pattern with containerRef/trackRef
  </action>
  <verify>
    <automated>npx vitest run src/lib/hooks/useHorizontalScroll.test.ts</automated>
  </verify>
  <done>
    Hook compiles without TypeScript errors. Existing unit tests pass. Debounce uses 200ms. No NodeJS.Timeout references remain. GSAP context includes invalidateOnRefresh: true.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden page.tsx horizontal scroll orchestration with image-load resilience</name>
  <files>src/app/page.tsx</files>
  <behavior>
    - Behavior 1: Container height is computed as travelDistance + window.innerHeight and set as minHeight to prevent dead scroll zones
    - Behavior 2: mounted state uses useRef-based client detection guard (avoids useEffect setState anti-pattern)
    - Behavior 3: Resize handler uses 200ms debounce (aligned with hook)
    - Behavior 4: Images in all horizontal sections trigger ScrollTrigger.refresh() after load via onLoad handler attached to each img element
    - Behavior 5: Horizontal scroll dispatches horizontal-scroll-progress and horizontal-scroll-hero events at proper frequency
    - Behavior 6: Skeleton loader remains visible until mounted + image containers reserved with aspect ratios where possible
  </behavior>
  <action>
    Update src/app/page.tsx to implement:
    1. Replace useEffect-based mounted state with useRef + useLayoutEffect pattern to avoid the synchronous setState anti-pattern per CONCERNS.md
    2. Add window.addEventListener('load') to call ScrollTrigger.refresh() after all images load (fallback safety)
    3. For each section's <img>, add onLoad={() => requestAnimationFrame(() => ScrollTrigger.refresh())} to recalc travelDistance per late-loading image
    4. Ensure container div has both height and minHeight set (minHeight = travelDistance + innerHeight * 2 is already in PATTERNS.md — apply it)
    5. Debounce resize handler to 200ms to match hook consistency
    6. Verify the CustomEvent dispatch for 'horizontal-scroll-progress' and 'horizontal-scroll-hero' remains in onUpdate
    7. Keep existing scrub: 1.2 value (Walker buttery feel)
  </action>
  <verify>
    <automated>npx next build --no-lint (or equivalent static export check)</automated>
  </verify>
  <done>
    page.tsx builds without errors. No more useEffect setState on mount (useRef guard used). Images have onLoad handlers to trigger ScrollTrigger refresh. Container minHeight uses travelDistance + innerHeight * 2. Resize debounce is 200ms.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (GSAP ScrollTrigger) → Browser Scroll Engine | Untrusted user scroll input controls scroll-driven animations; requires debouncing and refresh coordination to prevent layout thrashing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing (scroll event flood) | useHorizontalScroll resize handler | mitigate | Debounce at 200ms prevents rapid-fire ScrollTrigger refresh spam that could freeze the main thread |
| T-01-02 | Tampering (DOM mutation mid-scroll) | Image onLoad triggers | mitigate | ScrollTrigger.refresh() wrapped in requestAnimationFrame to batch DOM reads/writes; invalidateOnRefresh: true forces re-measurement |
| T-01-03 | Denial of Service (stuck pin) | GSAP context not reverted on hot-reload | mitigate | Cleanup returns animationContext.revert(); React StrictMode double-mount handled by context creation inside useEffect body |
</threat_model>

<verification>
Post-implementation checks:
- [ ] Scroll the full horizontal track — no whitespace at the far right, no overscroll bounce territory
- [ ] Resize the browser window across breakpoints (375px → 1920px) — scroll position remains stable, no jump, travelDistance recalculates
- [ ] Load page with DevTools Network throttling (Slow 3G) — images load progressively, scroll animation re-centers after each image load without jank
- [ ] Verify unit test: useHorizontalScroll.test.ts runs fully under Vitest, all tests pass
- [ ] Check TypeScript compilation: npx tsc --noEmit reports zero errors in modified files
</verification>

<success_criteria>
Success is measured by:
- Homepage horizontal scroll covers all 9 sections with exact Walker-style end-point alignment (no unreachable whitespace)
- Resizing the browser window does NOT cause scroll position loss or pin jitter
- Late-loading images (on slow connections) do NOT create scroll gaps — refresh is seamless
- Code passes TypeScript strict mode with zero type errors
- Existing useHorizontalScroll unit test suite passes unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-walker-fidelity/01-01-SUMMARY.md` documenting changes made, fix rationale, and any remaining open questions.
</output>

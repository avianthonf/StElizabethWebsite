---
phase: 01-foundation-walker-fidelity
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/sections/StickySplitSection.tsx
autonomous: false
requirements:
  - FND-05
  - FND-06
  - FND-08
  - FND-09
  - FND-10
user_setup: []
must_haves:
  truths:
    - "User sees homepage content sections that match the Walker-inspired visual system, including value cards, sticky split sections, passions panels, divisions tabs, and footer CTA"
    - "User experiences responsive layouts that preserve reference fidelity across mobile, tablet, and desktop breakpoints"
    - "User with prefers-reduced-motion enabled receives appropriate simplified animations across all sections"
    - "User does not experience major cumulative layout shift from late-loading images"
    - "Static export completes successfully and all homepage sections are present and functional"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage composition with all 9 sections, aspect-ratio-reserved images, onLoad refresh handlers"
      min_lines: 200
    - path: "src/components/sections/StickySplitSection.tsx"
      provides: "Full-viewport 45/55 sticky split section with accordion, responsive image sizing"
      min_lines: 150
    - path: "src/app/globals.css"
      provides: "Media query for prefers-reduced-motion that disables GSAP heavy animations safely"
      contains: "@media (prefers-reduced-motion: reduce)"
  key_links:
    - from: "StickySplitSection.tsx"
      to: "Framer Motion / CSS transition"
      via: "accordion open/close with maxHeight transition"
      pattern: "transition.*max-height"
    - from: "page.tsx"
      to: "globals.css"
      via: "reduce-motion class applied conditionally"
      pattern: "className.*reduce-motion"
---

<objective>
Deliver Walker-fidelity for all remaining homepage content sections (Value Cards, Sticky Split Sections, Passions Panels, Divisions Tabs, Footer CTA) and harden responsive behavior across mobile/tablet/desktop. Apply reduced-motion and CLS guardrails site-wide. Verify static export succeeds with all sections present.

Purpose: FND-05 requires all five Walker-inspired content patterns; FND-06 requires responsive fidelity; FND-08 requires reduced-motion across all animation-heavy sections; FND-09 requires CLS control for all images; FND-10 requires successful build. These are interdependent.

Output: Refactored page.tsx with all sections having aspect-ratio-reserved images and onLoad refresh; StickySplitSection.tsx with responsive image height fix and accordion state safety; globals.css with comprehensive reduced-motion overrides; build passes with zero errors.
</objective>

<execution_context>
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/workflows/execute-plan.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/templates/summary.md
</execution_context>

<context>
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/PROJECT.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/ROADMAP.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/REQUIREMENTS.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/STATE.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/phases/01-foundation-walker-fidelity/01-CONTEXT.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/phases/01-foundation-walker-fidelity/01-PATTERNS.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/codebase/STRUCTURE.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/codebase/CONCERNS.md

# Existing files this plan modifies
@D:/Users/Avinash/Documents/StElizabethWebsite/src/app/page.tsx
@D:/Users/Avinash/Documents/StElizabethWebsite/src/components/sections/StickySplitSection.tsx
@D:/Users/Avinash/Documents/StElizabethWebsite/src/app/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply image aspect-ratio reservation and onLoad refresh to all inline sections</name>
  <files>src/app/page.tsx</files>
  <behavior>
    - Every section wrapper (Sections 2-9) encloses images in container with aspect-ratio CSS to prevent CLS
    - Each img has onLoad handler triggering requestAnimationFrame(() => ScrollTrigger.refresh())
    - Inline helpers (ValueCard, PassionsPanel, DivisionsTabsHorizontal) use properly sized images
    - Placeholder images tagged for Phase 2 replacement
  </behavior>
  <action>
    Modify src/app/page.tsx:
    1. For VALUE CARDS: wrap image containers with aspect-ratio: 1
    2. For PASSIONS PANELS: wrap img in container with aspect-ratio: 16/9 and object-fit: contain
    3. For STICKY SPLIT SECTIONS: left image 65vh — reserve with aspect-ratio and explicit container height
    4. For DIVISIONS TABS: grade image set aspect-ratio: 4/3 and reserve width
    5. Add onLoad handlers: onLoad={() => requestAnimationFrame(() => { if (typeof ScrollTrigger !== 'undefined') ScrollTrigger.refresh(); })}
  </action>
  <verify>
    <automated>npx next build; TypeScript zero errors; ESLint no onLoad warnings</automated>
  </verify>
  <done>
    All images reserved with aspect-ratio; onLoad handlers call ScrollTrigger.refresh(); build passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix StickySplitSection responsive image sizing and overflow safety</name>
  <files>src/components/sections/StickySplitSection.tsx</files>
  <behavior>
    - Left image 65vh height never exceeds viewport; right images stack 55%/45% split without overflow
    - Mobile breakpoints (under 768px): image column stacks vertically; no horizontal scroll bleed
    - No horizontal scrollbar on any breakpoint; overflow hidden respected
    - Accordion expands without shifting content into overflow
  </behavior>
  <action>
    Update StickySplitSection.tsx:
    1. Replace hardcoded calc(65vh * 0.55 - 6px) and calc(65vh * 0.45 - 6px) with named constants (RIGHT_IMAGE_HEIGHT_TOP, RIGHT_IMAGE_HEIGHT_BOTTOM)
    2. Add responsive grid: small screens grid-cols-1 (stack), larger keep 45/55 split
    3. Ensure section wrapper overflow='hidden'; inner grid padding respects mobile safe rails
    4. Add descriptive aria-label to all images
    5. Extract common styles to Tailwind classes where practical; document style rationale
  </action>
  <verify>
    <automated>npm run build; zero TypeScript errors; verify on mobile (375px) that right-images stack vertically; no horizontal overflow</automated>
  </verify>
  <done>
    StickySplitSection images size correctly across breakpoints; no overflow; ARIA labels added; height values documented.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add comprehensive prefers-reduced-motion support across all sections</name>
  <files>src/app/page.tsx, src/components/sections/*.tsx</files>
  <behavior>
    - GSAP animation scrubbing disabled when prefers-reduced-motion active; horizontal scroll pinning works (scrub: 0)
    - Accordion open/close uses instant CSS (maxHeight snaps) not Framer Motion transitions
    - PassionsPanel and DivisionsTabs skip any transition/delay effects
    - All animations replaced with instant state when reduced-motion enabled
  </behavior>
  <action>
    Coordinate reduced-motion across Phase 1:
    1. Add usePrefersReducedMotion hook (create src/lib/hooks/usePrefersReducedMotion.ts if missing)
    2. In page.tsx: change scrub from 1.2 to prefersReducedMotion ? 0 : 1.2
    3. In StickySplitSection: accordion maxHeight transition becomes instant style update when reduced motion
    4. In PassionsPanel inline: image filter static; no transform animates
    5. In DivisionsTabsHorizontal inline: tab switching instant
    6. Audit Sections 6-9 inline helpers for motion; disable when reduce-motion active via conditional className='reduce-motion'
    7. Add @media (prefers-reduced-motion: reduce) overrides to globals.css
  </action>
  <verify>
    <automated>Build succeeds; DevTools prefers-reduced-motion toggle reload shows no GSAP scrub, accordions instant-expand</automated>
  </verify>
  <done>
    All GSAP and Framer Motion animations respect prefers-reduced-motion; reduced-motion mode functional; navigation unaffected.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Execute and verify Phase 1 homepage parity</name>
  <files>[]</files>
  <action>Human performs manual browser verification against the how-to-verify checklist below; no Claude automation required for this final validation step</action>
  <verify>Human reports "approved" with notes or identifies issues requiring revisiting</verify>
  <done>Human signals approval; all automated checks passed</done>
  <what-built>
  Phase 1 plan artifact set (01-01, 01-02, 01-03, 01-04) delivered. Core work includes hardened horizontal scroll-jacking, ghost header with dual-mode scrolling and mega-menu, hero masked typography with single-pulse, remaining sections with responsive layout, reduced-motion support, and aspect-ratio CLS guardrails.
  </what-built>
  <how-to-verify>
  **Automated checks first:**
  1. npm run build — zero errors, out/ directory contains all HTML
  2. npx vitest run — unit tests pass (particularly useHorizontalScroll hook)
  3. npx eslint src/ — zero critical warnings
  4. Build completes in under 60 seconds with clean output

  **Manual browser verification:**
  5. npm run dev → http://localhost:3000
  6. Homepage scroll: mouse wheel buttery smooth (scrub: 1.2), no jitter; all 9 sections reachable
  7. Resize 375 → 1920px: scroll position stable; header transparent→solid at 5%; no horizontal overflow
  8. Desktop header: Hover Academics — mega-menu appears after 100ms, displays child items, closes on exit
  9. Mobile (375px): Tap hamburger → menu slides in; body scroll locked via RemoveScroll; Escape closes; close X works
  10. Hero at top: "WE BELIEVE" zooms scale 60→1 during first 12% horizontal scroll; mission block visible; no CLS on slow-3G
  11. Reduced motion: DevTools Rendering → Emulate prefers-reduced-motion=reduce → reload; verify no GSAP scrubbing, hero static, accordions instant
  12. Safari (if available): scroll-pin jank-free, hero SVG text crisp at 125% zoom
  </how-to-verify>
  <resume-signal>
  Type "approved" if automated checks pass and manual spot-checks confirm header, hero, scroll, responsive states, and reduced-motion support are substantially Walker-fidelity. Include any revisiting notes.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side scroll metrics → GSAP animation | User-generated scroll events drive animation; debounced to prevent layout thrashing |
| Static export output → Production hosting | Build artifacts work without server-side runtime; all client-side code self-contained |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Spoofing (layout measurement) | ScrollTrigger end calculations | mitigate | End uses dynamic travelDistance + innerHeight × 2 buffer; re-calculated on image load |
| T-01-10 | DoS (oversized images) | CLS from late-loaded assets | mitigate | Aspect-ratio divs reserve space before network fetch; images already WebP-optimized |
| T-01-11 | Tampering (resize flood) | Resize event spam | mitigate | Debounce 200ms at multiple layers; ScrollTrigger.ignoreMobileResize enabled in gsap-config |
</threat_model>

<verification>
Phase 1 automated guardrails:
- [ ] npm run build < 60 seconds
- [ ] out/index.html contains all 9 section wrappers
- [ ] Lighthouse Performance ≥ 90 (CLS < 0.1, INP < 200ms) desktop
- [ ] Zero console errors (ESLint + runtime)
- [ ] Vitest unit tests pass for useHorizontalScroll
- [ ] Responsive breakpoints verified: 375px, 768px, 1024px, 1440px
</verification>

<success_criteria>
All five Walker homepage content patterns (FND-05) present and functional. Responsive layouts (FND-06) intact at all breakpoints. Reduced-motion (FND-08) and CLS (FND-09) guardrails universally applied. Static build (FND-10) succeeds with zero errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-walker-fidelity/01-04-SUMMARY.md` summarizing section parity fixes, responsive testing outcomes, reduced-motion coverage, CLS improvements, and build verification.
</output>

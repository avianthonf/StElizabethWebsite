---
phase: 02-content-interior-pages
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - src/app/academics/page.tsx
  - src/app/admissions/page.tsx
  - src/app/athletics/page.tsx
  - src/app/arts/page.tsx
  - src/app/contact/page.tsx
autonomous: true
requirements:
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
  - PAGE-06

must_haves:
  truths:
    - User can navigate to /academics and see divisions overview
    - User can navigate to /admissions and see inquiry process
    - User can navigate to /athletics and see athletics program overview
    - User can navigate to /arts and see arts program overview
    - User can navigate to /contact and see contact information with map
  artifacts:
    - path: src/app/academics/page.tsx
      provides: Academics page with divisions overview
      contains: "St. Elizabeth"
      min_lines: 40
    - path: src/app/admissions/page.tsx
      provides: Admissions page with inquiry form and process timeline
      contains: "admission"
      min_lines: 50
    - path: src/app/athletics/page.tsx
      provides: Athletics page with cutout panel style sections
      contains: "athletics"
      min_lines: 40
    - path: src/app/arts/page.tsx
      provides: Arts page with cutout panel style sections
      contains: "arts"
      min_lines: 40
    - path: src/app/contact/page.tsx
      provides: Contact page with form and map embed
      contains: "Pomburpa"
      min_lines: 50
  key_links:
    - from: src/app/academics/page.tsx
      to: src/components/templates/ContentPage.tsx
      via: template usage
      pattern: "ContentPage"
    - from: src/app/admissions/page.tsx
      to: src/components/templates/ContentPage.tsx
      via: template usage
      pattern: "ContentPage"
    - from: src/lib/site-navigation.ts
      to: src/app/academics/page.tsx
      via: navigation href
      pattern: "/academics"

user_setup: []
---

<objective>
Build the remaining five interior pages (Academics, Admissions, Athletics, Arts, Contact) using the ContentPage template established in Plan 02-03.

Purpose: With the template system in place and About page as proof-of-concept, we can now rapidly build the remaining interior pages. Each page reuses the ContentPage template for consistent layout while showcasing different content types: divisions overview (Academics), inquiry form (Admissions), cutout panels (Athletics/Arts), and contact form with map (Contact).

Output: Five new interior pages accessible via navigation, all using consistent St. Elizabeth branding and the ContentPage template pattern.
</objective>

<execution_context>
@C:/Users/Avinash/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Avinash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/02-content-interior-pages/02-03-PLAN.md
@src/components/templates/ContentPage.tsx
@src/lib/site-navigation.ts
@src/app/page.tsx
@src/components/sections/StickySplitSection.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/lib/site-navigation.ts:
```typescript
export interface NavItem {
  label: string;
  href: string;
  children?: NavItem[];
}

export const siteNavigation: NavItem[];
export function getNavItemByHref(href: string): NavItem | null;
export function getBreadcrumbs(href: string): NavItem[];
```

From src/components/templates/ContentPage.tsx (created in Plan 02-03):
```typescript
interface ContentPageProps {
  title: string;
  overline?: string;
  children: React.ReactNode;
  heroImage?: string;
  backgroundColor?: string;
}

export function ContentPage(props: ContentPageProps): JSX.Element;
```

From src/components/sections/StickySplitSection.tsx:
```typescript
interface AccordionItem {
  title: string;
  content: string;
}

interface StickySplitSectionProps {
  overline: string;
  heading: string;
  body: string;
  accordion: AccordionItem[];
  leftImage: string | null;
  rightImages: string[];
  backgroundColor?: string;
}

export function StickySplitSection(props: StickySplitSectionProps): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build Academics page with divisions overview</name>
  <files>src/app/academics/page.tsx</files>
  <action>
Create `src/app/academics/page.tsx` using ContentPage template. Structure:
- Use ContentPage wrapper with title "Academics" and overline "Excellence in Education"
- Include hero image from public/images/ (reuse existing school photo or use campus image)
- Add 2-3 sections describing St. Elizabeth's academic approach:
  - Section 1: Academic philosophy paragraph (faith-integrated, college-preparatory, whole-person development)
  - Section 2: Divisions overview using StickySplitSection component with accordion for Primary, Lower, Middle, Upper School divisions
  - Section 3: Curriculum highlights (STEM, humanities, languages, arts integration)
- Use existing StickySplitSection component pattern from homepage (see src/app/page.tsx lines 117-141)
- Reference navigation structure from src/lib/site-navigation.ts (Academics is at /academics with children /academics/curriculum, etc.)
- Ensure all copy is St. Elizabeth-specific (mention Pomburpa, Goa, Catholic education, faith values)
- Use CSS custom properties for colors (--color-primary-maroon, --color-white)
- Export default function Academics() returning ContentPage with sections
  </action>
  <verify>
    <automated>npm run build && grep -q "Academics" out/academics/index.html</automated>
  </verify>
  <done>Academics page exists at src/app/academics/page.tsx, builds successfully, uses ContentPage template, contains St. Elizabeth academic content with divisions overview</done>
</task>

<task type="auto">
  <name>Task 2: Build Admissions and Contact pages with forms</name>
  <files>src/app/admissions/page.tsx, src/app/contact/page.tsx</files>
  <action>
Create two pages with form elements:

**src/app/admissions/page.tsx:**
- Use ContentPage template with title "Admissions" and overline "Join Our Community"
- Section 1: Welcome paragraph (St. Elizabeth welcomes families seeking faith-based education)
- Section 2: Admission process timeline (3-4 steps: Inquiry → Visit → Application → Enrollment)
- Section 3: Inquiry form placeholder (for now, add a note: "Form integration in Phase 3 - FORM-01 through FORM-05")
  - Include form structure with fields: name, email, phone, student grade level, message
  - Add disabled submit button with text "Form submission coming in Phase 3"
  - Use semantic HTML: `<form>`, `<label>`, `<input>`, `<textarea>`, `<button>`
  - Style with Tailwind classes matching site design (Montserrat font, maroon accents)
- Link to /admission/process, /admission/fees from navigation

**src/app/contact/page.tsx:**
- Use ContentPage template with title "Contact Us" and overline "Get in Touch"
- Section 1: School address (St. Elizabeth High School, Pomburpa, Goa, India - use actual address if known, or placeholder)
- Section 2: Contact information (phone, email, office hours)
- Section 3: Contact form (same structure as admissions form, disabled for Phase 3)
- Section 4: Map embed placeholder (add `<div>` with text "Google Maps embed coming in Phase 3" and min-height: 400px, background: #f5f5f5)
- Ensure accessibility: proper labels, aria-labels, keyboard navigation

Both pages should:
- Use ContentPage template consistently
- Include St. Elizabeth branding and copy
- Note that forms are non-functional until Phase 3 (FORM-01 through FORM-05 requirements)
- Build successfully with `npm run build`
  </action>
  <verify>
    <automated>npm run build && grep -q "Admissions" out/admissions/index.html && grep -q "Contact" out/contact/index.html</automated>
  </verify>
  <done>Admissions page at src/app/admissions/page.tsx and Contact page at src/app/contact/page.tsx exist, use ContentPage template, contain form structures (disabled), build successfully</done>
</task>

<task type="auto">
  <name>Task 3: Build Athletics and Arts pages with cutout panel style</name>
  <files>src/app/athletics/page.tsx, src/app/arts/page.tsx</files>
  <action>
Create two pages using cutout panel pattern from homepage (see src/app/page.tsx lines 144-195 PassionsPanel component):

**src/app/athletics/page.tsx:**
- Use ContentPage template with title "Athletics" and overline "Excellence in Competition"
- Section 1: Athletics philosophy paragraph (character through competition, sportsmanship, teamwork)
- Section 2: Sports offerings overview (list 8-10 sports: football, basketball, volleyball, cricket, athletics, swimming, etc.)
- Section 3: Cutout panel style sections (reuse PassionsPanel pattern):
  - Panel 1: Team Sports (maroon background, white text, cutout image of team sport)
  - Panel 2: Individual Sports (white background, dark text, cutout image of individual athlete)
  - Panel 3: Facilities (dark background, white text, cutout image of sports facility)
- Use existing images from public/images/ or reference placeholder paths
- Ensure WCAG AA contrast ratios (same as homepage PassionsPanel)

**src/app/arts/page.tsx:**
- Use ContentPage template with title "Arts" and overline "Creativity & Expression"
- Section 1: Arts philosophy paragraph (creativity flourishes, expression, appreciation for beauty)
- Section 2: Arts programs overview (visual arts, music, drama, dance)
- Section 3: Cutout panel style sections:
  - Panel 1: Visual Arts (white background, dark text, cutout image of student artwork)
  - Panel 2: Performing Arts (maroon background, white text, cutout image of theatre/music)
  - Panel 3: Arts Facilities (dark background, white text, cutout image of art studio/theatre)
- Reuse PassionsPanel component pattern or inline similar structure
- Link to /activities/arts from navigation

Both pages should:
- Use ContentPage template wrapper
- Include cutout panel sections matching homepage style
- Use St. Elizabeth-specific copy (mention faith integration in arts, character development through athletics)
- Build successfully with `npm run build`
  </action>
  <verify>
    <automated>npm run build && grep -q "Athletics" out/athletics/index.html && grep -q "Arts" out/arts/index.html</automated>
  </verify>
  <done>Athletics page at src/app/athletics/page.tsx and Arts page at src/app/arts/page.tsx exist, use ContentPage template, include cutout panel style sections, build successfully</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Static Pages | No trust boundary - static content only, no user input processed in Phase 2 |
| Browser → Public Assets | Static images and files served directly, no dynamic content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Contact page address | accept | Public school address is intentionally public information |
| T-02-02 | Denial of Service | Large images | mitigate | Images already optimized in Phase 1 (FOUND-07, FOUND-08) |
| T-02-03 | Spoofing | Form placeholders | accept | Forms are disabled/non-functional until Phase 3, no spoofing risk |

Note: Phase 2 creates static pages only. Form security (CSRF, XSS, injection) will be addressed in Phase 3 (FORM-01 through FORM-05).
</threat_model>

<verification>
After completing all tasks:

1. **Build verification**: Run `npm run build` - must succeed with no errors
2. **Route verification**: Check that all 5 pages are generated in `out/` directory:
   - `out/academics/index.html`
   - `out/admissions/index.html`
   - `out/athletics/index.html`
   - `out/arts/index.html`
   - `out/contact/index.html`
3. **Navigation verification**: Check that src/lib/site-navigation.ts hrefs match created pages
4. **Template usage verification**: Grep for `ContentPage` import in all 5 page files
5. **Branding verification**: Grep for "St. Elizabeth" in all 5 page files
6. **Accessibility verification**: Run existing Playwright accessibility tests (from Phase 1) on new pages
</verification>

<success_criteria>
Phase 2 Plan 04 is complete when:

- [ ] Academics page exists at src/app/academics/page.tsx with divisions overview using StickySplitSection
- [ ] Admissions page exists at src/app/admissions/page.tsx with inquiry form structure (disabled, Phase 3 note)
- [ ] Athletics page exists at src/app/athletics/page.tsx with cutout panel style sections
- [ ] Arts page exists at src/app/arts/page.tsx with cutout panel style sections
- [ ] Contact page exists at src/app/contact/page.tsx with contact form (disabled) and map placeholder
- [ ] All 5 pages use ContentPage template consistently
- [ ] All 5 pages contain St. Elizabeth-specific copy (no Walker School references)
- [ ] All 5 pages build successfully with `npm run build`
- [ ] All 5 pages are accessible via navigation links in src/lib/site-navigation.ts
- [ ] All 5 pages pass existing Playwright accessibility tests (WCAG 2.1 AA)
- [ ] Forms are clearly marked as non-functional with Phase 3 notes
- [ ] No console errors or build warnings
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-interior-pages/02-04-SUMMARY.md` documenting:
- Which 5 pages were created
- How ContentPage template was reused
- Which requirements were fulfilled (PAGE-02 through PAGE-06)
- Any deviations from plan or issues encountered
- Context cost estimate for this plan execution
</output>

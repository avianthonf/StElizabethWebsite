---
phase: 04-seo-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - public/sitemap.xml
  - public/robots.txt
autonomous: true
requirements:
  - SEO-01
  - SEO-03
  - SEO-04

must_haves:
  truths:
    - Search engines can discover all public pages via sitemap.xml
    - Google Search Console can read structured data about St. Elizabeth High School
    - Search engine crawlers are allowed to index the site
  artifacts:
    - path: src/app/layout.tsx
      provides: JSON-LD structured data in <head>
      contains: "application/ld+json"
    - path: public/sitemap.xml
      provides: List of all public URLs with lastmod dates
      min_lines: 10
    - path: public/robots.txt
      provides: Crawler directives allowing indexing
      contains: "User-agent: *"
  key_links:
    - from: src/app/layout.tsx
      to: JSON-LD script tag
      via: dangerouslySetInnerHTML
      pattern: "dangerouslySetInnerHTML.*application/ld\\+json"
    - from: public/sitemap.xml
      to: All page routes
      via: Static XML file
      pattern: "<loc>https://stelizabethhighschool\\.edu\\.in/"
---

<objective>
Add SEO infrastructure for search engine discovery and rich results.

Purpose: Enable Google and other search engines to understand St. Elizabeth High School as an educational institution, discover all pages, and display rich snippets in search results.

Output: JSON-LD structured data in layout, static sitemap.xml with all public pages, robots.txt allowing crawlers.
</objective>

<execution_context>
@C:/Users/Avinash/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Avinash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/ROADMAP.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/STATE.md
@D:/Users/Avinash/Documents/StElizabethWebsite/.planning/REQUIREMENTS.md
@D:/Users/Avinash/Documents/StElizabethWebsite/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSON-LD structured data to layout</name>
  <files>src/app/layout.tsx</files>
  <action>
Add JSON-LD structured data to RootLayout component combining LocalBusiness and EducationalOrganization schemas per schema.org spec.

In the <head> section (via Next.js metadata or script tag in body), add a script tag with type="application/ld+json" containing:

```json
{
  "@context": "https://schema.org",
  "@type": ["EducationalOrganization", "HighSchool", "LocalBusiness"],
  "name": "St. Elizabeth High School",
  "alternateName": "St. Elizabeth HS Pomburpa",
  "description": "A Catholic high school in Pomburpa, Goa, India, dedicated to academic excellence, faith formation, and service.",
  "url": "https://stelizabethhighschool.edu.in",
  "logo": "https://stelizabethhighschool.edu.in/images/logo.png",
  "image": "https://stelizabethhighschool.edu.in/images/hero-campus.jpg",
  "telephone": "+91-832-XXXXXXX",
  "email": "info@stelizabethhighschool.edu.in",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Pomburpa",
    "addressLocality": "Bardez",
    "addressRegion": "Goa",
    "postalCode": "403521",
    "addressCountry": "IN"
  },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": 15.5626,
    "longitude": 73.8143
  },
  "sameAs": [
    "https://www.facebook.com/stelizabethhspomburpa",
    "https://www.instagram.com/stelizabethhspomburpa"
  ],
  "foundingDate": "1985",
  "priceRange": "₹₹"
}
```

Use placeholder values for telephone, email, and social media URLs (user will update with real values). Add comment noting these need user configuration.

Place the script tag in the body (after opening <body> tag) using dangerouslySetInnerHTML since Next.js metadata API doesn't support JSON-LD directly.
  </action>
  <verify>
    <automated>npm run build && grep -q "application/ld+json" out/index.html</automated>
  </verify>
  <done>JSON-LD script tag present in layout.tsx, build succeeds, structured data appears in static HTML output</done>
</task>

<task type="auto">
  <name>Task 2: Create static sitemap.xml</name>
  <files>public/sitemap.xml</files>
  <action>
Create static sitemap.xml in public/ directory listing all public pages with priority and lastmod.

Include these URLs (all with https://stelizabethhighschool.edu.in prefix):
- / (priority 1.0, lastmod today's date)
- /about (priority 0.8)
- /academics (priority 0.8)
- /admissions (priority 0.9)
- /athletics (priority 0.7)
- /arts (priority 0.7)
- /contact (priority 0.9)

Use standard sitemap XML format:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://stelizabethhighschool.edu.in/</loc>
    <lastmod>2026-04-27</lastmod>
    <priority>1.0</priority>
  </url>
  <!-- ... other URLs -->
</urlset>
```

Set lastmod to 2026-04-27 for all pages (current date). User can update manually when content changes or automate with build script later.
  </action>
  <verify>
    <automated>test -f public/sitemap.xml && grep -q "stelizabethhighschool.edu.in" public/sitemap.xml</automated>
  </verify>
  <done>sitemap.xml exists in public/, contains all 7 public pages with valid XML structure</done>
</task>

<task type="auto">
  <name>Task 3: Create robots.txt</name>
  <files>public/robots.txt</files>
  <action>
Create robots.txt in public/ directory allowing all search engine crawlers.

Content:
```
User-agent: *
Allow: /

Sitemap: https://stelizabethhighschool.edu.in/sitemap.xml
```

This allows all crawlers (Google, Bing, etc.) to index all pages and points them to the sitemap for efficient discovery.
  </action>
  <verify>
    <automated>test -f public/robots.txt && grep -q "Allow: /" public/robots.txt</automated>
  </verify>
  <done>robots.txt exists in public/, allows all crawlers, references sitemap.xml</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `npm run build` - build succeeds without errors
2. Check `out/sitemap.xml` and `out/robots.txt` exist (copied from public/)
3. Check `out/index.html` contains JSON-LD script tag with EducationalOrganization schema
4. Validate sitemap.xml at https://www.xml-sitemaps.com/validate-xml-sitemap.html
5. Validate JSON-LD at https://validator.schema.org/
</verification>

<success_criteria>
- JSON-LD structured data present in layout with EducationalOrganization + LocalBusiness types
- sitemap.xml lists all 7 public pages with valid XML structure
- robots.txt allows crawler access and references sitemap
- Static export builds successfully with all SEO files in out/ directory
- Schema.org validator shows no errors for JSON-LD markup
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-polish/04-01-SUMMARY.md`
</output>
